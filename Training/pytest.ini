[pytest]
testpaths = tests
python_files = test_*.py
markers =
    cpp: requires the chessmimic_core C++ extension
    data: requires tracked sample data or generated local data
    slow: longer-running regression or integration coverage
